<div class="greenback">
  <h1>In reply to "<%= link_to @post.title, @post %>" </h1>
  <p class="whiteback">
    by <span class="limegreen"><%= @reply.user.email %></span> on <%= @reply.created_at.strftime("%A, %B %e, %Y") %>
  </p>
  <p><%= @reply.content %></p>
</div>
