<!DOCTYPE html>
<html>
<head>
  <title>Julie's Messageboard</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
  <body>
    <% if current_user %>
      <div class="navbar navbar-default">
        <div class="container">
          <div class="navbar-header">
            <a class="navbar-brand">
              <span class="glyphicon glyphicon-sunglasses" aria-hidden="true"></span>
              <%= link_to "Message Board", posts_path, class: "navbar-brand" %>
            </a>
          </div>
          <ul class="nav navbar-nav">
            <li class="active">
              <a href="/">
                <span class="glyphicon glyphicon-home" aria-hidden="true"></span> Home
              </a>
            </li>
          </ul>
          <p class="navbar-text pull-right">
            <%= current_user.email %> | <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
          </p>
        </div>
      </div>
    <% else %>
    <% end %>
    <div class="container">
      <% if notice %>
        <p class="alert alert-info"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-warning"><%= alert %></p>
      <% end %>
      <%= yield %>
    </div>
    <div class="footer">
      <a href="http://julemagne.github.io/portfolio/">Julie David</a> <%= Date.today.year %> ◕ ‿ ◕
    </div>
  </body>
</html>
